#!/usr/bin/sh

set -e
uid=$(id -u user)
case $uid in
(*[!0-9]*) echo "Fatal error: Bad UID for 'user'" >&2; exit 1;;
esac
sudo bash -c "mkdir -p /builder/incoming && chown -R '$uid:$uid' /builder"
exec /usr/lib/qubes/qfile-unpacker "$uid" '/builder/incoming'
