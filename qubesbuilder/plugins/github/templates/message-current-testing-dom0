**Automated announcement from [builder-github]**

The component `@COMPONENT@` (including package `@PACKAGE_NAME@`) has been pushed to the `@RELEASE_NAME@` testing repository for dom0.
To test this update, please install it with the following command:
```
sudo qubes-dom0-update --enablerepo=qubes-dom0-current-testing
```
[Changes included in this update][commits]

[builder-github]: https://github.com/QubesOS/qubes-builder-github
[commits]: @GIT_LOG_URL@
